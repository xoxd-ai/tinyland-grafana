name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  actions: read
  contents: read
  packages: read
jobs:
  package:
    uses: xoxd-ai/ci-templates/.github/workflows/js-bazel-package.yml@v2.14.1
    with:
      runner_mode: repo_owned
      runner_labels_json: ${{ vars.PRIMARY_LINUX_RUNNER_LABELS_JSON }}
      workspace_mode: isolated
      publish_mode: hosted_exception
      npm_publish_mode: disabled
      node_versions: '["22"]'
      publish_node_version: "22"
      pnpm_version: "10.13.1"
      # npm-resolution retired; validate via bazel (TIN-1716)
      typecheck_command: ""
      unit_test_command: ""
      build_command: "true"
      package_check_command: "true"
      bazel_targets: "//:pkg //:test"
      package_dir: ./bazel-bin/pkg
      npm_access: public
      github_package_name: "@tinyland-inc/tinyland-grafana"
      dry_run: true
    secrets: inherit
